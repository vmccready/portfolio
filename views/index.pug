extends layout.pug

block content 
  article.grid-container.portfolio-intro
    div

      h1 Welcome to My Portfolio

      p.lead.text-light
        | Here is my portfolio of projects completed for the TeamTreehouse.com Techdegree curriculum. The projects are designed to prepare developers to be job ready, 
        | so all Javascript code is written by me. This portfolio itself is a project to demonstrate server side development using Node.js and the Express framework!
  article.grid-container.portfolio-index
    
    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3

      each project in projects
        .cell
          a(href=`/project/${project.id}`)
            img.thumbnail(src=`static/img/project${project.id + 1}-main.jpg`, height='550', width='550')
            h5 #{project.project_name}
      
